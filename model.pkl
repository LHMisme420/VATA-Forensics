
# dummy_model.py (run this once, then delete)
import pickle
from sklearn.linear_model import LogisticRegression  # or any simple model
model = LogisticRegression()  # placeholder
with open("model.pkl", "wb") as f:
    pickle.dump(model, f)
